apiVersion: v1
kind: Service
metadata:
  name: claw-device-pairing
  labels:
    app: claw-device-pairing
spec:
  type: ClusterIP
  selector:
    app: claw-device-pairing
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
